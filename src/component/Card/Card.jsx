import React from "react";

const Card = ({ props }) => {
  return (
    <div className="card">
      {/* <h3>{props.name}</h3>
      <p>Type: {props.propertyType}</p> */}
      <h1>hello</h1>
    </div>
  );
};

export default Card;
